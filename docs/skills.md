# Skills

Vegvisir supports local skills, LSL routing, Skiller bundles, evals, forge/patch workflows, and trace promotion. Use `/skills status`, `/skills compile`, `/skills route <query>`, `/skills load <query>`, `/skills eval`, and `/skills trace`.
