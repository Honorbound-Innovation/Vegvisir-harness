# MCP Services

MCP tools extend Vegvisir through configured local or HTTP services. Authenticated services should use HBSE-backed service refs.

Commands: `/mcp list`, `/mcp status`, `/mcp tools`, `/mcp add-http-service`, `/hbse service add`.
