{
  "name": "vegvisir-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop shell for the Vegvisir agentic development harness.",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "check": "tsc --noEmit",
    "web:build": "vite build",
    "web:dev": "vite --host 127.0.0.1 --port 1420"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
